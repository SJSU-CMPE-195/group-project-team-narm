This folder contains files that are not used in the current PoC pipeline.

PoC runtime files live in the parent `jetson-code/` folder (TCP ingest → decode → MediaPipe Holistic → LSTM → print gloss).

